{
  "name": "low-pass-fft-app",
  "version": "1.0.0",
  "description": "Fourier Transform Image Filter - Backend + Frontend",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["fft", "image-processing", "fourier-transform", "react", "express"],
  "author": "",
  "license": "MIT"
}
